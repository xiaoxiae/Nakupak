[project]
name = "llm-worker"
version = "0.1.0"
description = "Remote LLM worker for Listom recipe import"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "websockets>=12.0",
]

[project.scripts]
llm-worker = "llm_worker:main"
